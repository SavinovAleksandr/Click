# Click v.5 - Dependencies (reconstructed from PyInstaller extraction)
# Python 3.10+ recommended (original built with Python 3.14)

# GUI
PyQt5>=5.15.0

# Document processing
python-docx>=0.8.11

# DXF/CAD
ezdxf>=1.0.0

# Image/plotting
matplotlib>=3.5.0
Pillow>=9.0.0

# Windows COM (pywin32) - required for Rastr and Word COM
# Install on Windows only: pip install pywin32
pywin32>=305; sys_platform == 'win32'

# Utilities
numpy>=1.20.0
